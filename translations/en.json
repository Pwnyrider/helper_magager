{
  "config": {
    "step": {
      "user": {
        "title": "Helper Manager",
        "description": "Add the Helper Manager integration to enable services for creating helper entities."
      }
    },
    "abort": {
      "single_instance_allowed": "Already configured. Only a single instance is allowed."
    }
  }
}
